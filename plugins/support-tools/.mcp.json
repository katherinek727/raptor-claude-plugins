{
  "mcpServers": {
    "scripts": {
      "command": "ruby",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-servers/gitlab-scripts/server.rb"],
      "env": {
        "GITLAB_TOKEN": "${GITLAB_TOKEN}"
      }
    }
  }
}
